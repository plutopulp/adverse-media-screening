# =============================================================================
# DEFAULT CONFIGURATION - COMMITTED TO GIT
# =============================================================================
# This file contains all non-secret configuration with sensible defaults.
# Override any settings in .env.secrets (which is gitignored).

# AI Service Connection
# This uses the Docker Compose service name for internal network communication
AI_SERVICE_URL=http://ai:5001

# Node Environment
# Override in docker-compose.yml or .env.secrets if needed
NODE_ENV=production
