# =============================================================================
# DEFAULT CONFIGURATION - COMMITTED TO GIT
# =============================================================================
# This file contains all non-secret configuration with sensible defaults.
# Override any settings in .env.secrets (which is gitignored).

# LLM Provider Configuration
# Note: Override your API keys in .env.secrets
OPENAI__MODEL=gpt-4o
OPENAI__TEMPERATURE=0.0
OPENAI__API_KEY=your-openai-api-key

ANTHROPIC__MODEL=claude-sonnet-4-5
ANTHROPIC__TEMPERATURE=0.0
ANTHROPIC__API_KEY=your-anthropic-api-key

# Default LLM provider to use (openai or anthropic)
DEFAULT_LLM_PROVIDER=openai

# Application Settings
ENVIRONMENT=development
LOG_LEVEL=INFO
